import {LidMatPaginatorIntl} from "./lid-mat-paginator-intl";

describe("LidMatPaginatorIntl", () => {
  it("should create an instance", () => {
    expect(new LidMatPaginatorIntl()).toBeTruthy();
  });
});
