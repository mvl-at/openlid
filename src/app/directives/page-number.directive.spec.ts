import {PageNumberDirective} from "./page-number.directive";

describe("PageNumberDirective", () => {
  it("should create an instance", () => {
    const directive = new PageNumberDirective();
    expect(directive).toBeTruthy();
  });
});
